#!/usr/bin/env node
const argv=process.argv.slice(2);let server=process.env.VIQ_SERVER??'http://127.0.0.1:7373';let jsonOutput=false;for(let i=0;i<argv.length;){if(argv[i]==='--json'){jsonOutput=true;argv.splice(i,1);}else if(argv[i]==='--server'){server=argv[i+1];argv.splice(i,2);}else i++;}
const usage=(m='invalid command')=>{throw Object.assign(new Error(m),{usage:true,code:'usage_error'});};const option=(n,{required=false}={})=>{const i=argv.indexOf(n),v=i>=0?argv[i+1]:undefined;if(required&&!v)usage(`${n} is required`);return v;};const credentials=()=>({claim_id:option('--claim-id',{required:true}),actor:option('--actor',{required:true}),claim_token:option('--claim-token',{required:true}),generation:Number(option('--generation',{required:true}))});const optionalCredentials=()=>{const claim_id=option('--claim-id'),claim_token=option('--claim-token'),generation=option('--generation');return{...(claim_id?{claim_id}:{}),...(claim_token?{claim_token}:{}),...(generation?{generation:Number(generation)}:{})};};const target=(prefix)=>{const actor=option(`--${prefix}`),role=option(`--${prefix}-role`);if(Boolean(actor)===Boolean(role))usage(`exactly one --${prefix} or --${prefix}-role is required`);return{type:role?'role':'actor',id:role??actor};};const auth=(headers)=>headers.authorization=`Bearer ${option('--auth',{required:true})}`;
async function command(){const[noun,verb,...p]=argv;let method='GET',route,body;const headers={};
if(noun==='actor'&&verb==='create'&&p[0]){method='POST';route='/v1/actors';body={id:p[0],name:option('--name',{required:true}),kind:option('--kind')??'agent',machine:option('--machine')};auth(headers);}else if(noun==='actor'&&verb==='list'){const q=new URLSearchParams();if(option('--active')!==undefined)q.set('active',option('--active'));route=`/v1/actors?${q}`;}else if(noun==='actor'&&verb==='show'&&p[0])route=`/v1/actors/${encodeURIComponent(p[0])}`;else if(noun==='actor'&&verb==='update'&&p[0]){method='PATCH';route=`/v1/actors/${encodeURIComponent(p[0])}`;body={...(option('--name')!==undefined?{name:option('--name')}:{}),...(option('--kind')!==undefined?{kind:option('--kind')}:{}),...(option('--machine')!==undefined?{machine:option('--machine')||null}:{}),...(option('--active')!==undefined?{active:option('--active')!=='false'}:{})};auth(headers);}else if(noun==='actor'&&verb==='deactivate'&&p[0]){method='POST';route=`/v1/actors/${encodeURIComponent(p[0])}/deactivate`;auth(headers);}else if(noun==='actor'&&verb==='inbox'&&p[0])route=`/v1/actors/${encodeURIComponent(p[0])}/inbox?after=${encodeURIComponent(option('--after')??'0')}`;
else if(noun==='role'&&verb==='create'&&p[0]){method='POST';route='/v1/roles';body={id:p[0],name:option('--name',{required:true})};auth(headers);}else if(noun==='role'&&verb==='list'&&!p[0])route='/v1/roles';else if(noun==='role'&&verb==='list'&&p[0])route=`/v1/actors/${encodeURIComponent(p[0])}/roles`;else if(noun==='role'&&verb==='members'&&p[0])route=`/v1/roles/${encodeURIComponent(p[0])}/actors`;else if(noun==='role'&&['grant','revoke','add'].includes(verb)&&p[0]&&p[1]){method=verb==='revoke'?'DELETE':'PUT';route=`/v1/actors/${encodeURIComponent(p[0])}/roles/${encodeURIComponent(p[1])}`;auth(headers);}
else if(noun==='project'&&verb==='create'&&p[0]){method='POST';route='/v1/projects';body={key:p[0]};}else if(noun==='project'&&verb==='list')route='/v1/projects';
else if(noun==='ticket'&&verb==='create'&&p[0]&&p[1]){method='POST';route='/v1/tickets';body={project:p[0],title:p[1],body:option('--body')??'',...(option('--assignee')||option('--assignee-role')?{assignee:target('assignee')}:{})};}else if(noun==='ticket'&&verb==='list'&&p[0]){const q=new URLSearchParams();if(option('--assignee')){q.set('assignee_type','actor');q.set('assignee_id',option('--assignee'));}if(option('--assignee-role')){q.set('assignee_type','role');q.set('assignee_id',option('--assignee-role'));}if(option('--unassigned')!==undefined)q.set('assignee_type','none');route=`/v1/projects/${encodeURIComponent(p[0])}/tickets?${q}`;}else if(noun==='ticket'&&verb==='show'&&p[0])route=`/v1/tickets/${encodeURIComponent(p[0])}`;else if(noun==='ticket'&&verb==='next'){const q=new URLSearchParams();if(option('--project'))q.set('project',option('--project'));if(option('--actor'))q.set('actor',option('--actor'));route=`/v1/tickets/next?${q}`;}else if(noun==='ticket'&&verb==='edit'&&p[0]){method='PATCH';route=`/v1/tickets/${encodeURIComponent(p[0])}`;body={actor:option('--actor'),...(option('--title')!==undefined?{title:option('--title')}:{}),...(option('--body')!==undefined?{body:option('--body')}:{}),...(option('--assignee')||option('--assignee-role')?{assignee:target('assignee')}:{}),...(argv.includes('--unassigned')?{assignee:null}:{})};}
else if(noun==='ticket'&&['claim','verify','release','submit'].includes(verb)&&p[0]){method='POST';route=`/v1/tickets/${encodeURIComponent(p[0])}/${verb}`;body=verb==='claim'?{actor:option('--actor',{required:true})}:{...credentials(),...(verb==='submit'?{reviewer:target('reviewer'),...(option('--message')?{message:option('--message')}:{})}:{})};}
else if(noun==='question'&&verb==='ask'&&p[0]){method='POST';route=`/v1/tickets/${encodeURIComponent(p[0])}/questions`;const t=target('target');body={...credentials(),kind:'text',text:option('--text',{required:true}),target_type:t.type,target_id:t.id};}else if(noun==='question'&&verb==='list'&&p[0])route=`/v1/tickets/${encodeURIComponent(p[0])}/questions${option('--status')?`?status=${encodeURIComponent(option('--status'))}`:''}`;else if(noun==='question'&&verb==='show'&&p[0]&&p[1])route=`/v1/tickets/${encodeURIComponent(p[0])}/questions`;else if(noun==='question'&&verb==='answer'&&p[0]&&p[1]){method='POST';route=`/v1/tickets/${encodeURIComponent(p[0])}/questions/${encodeURIComponent(p[1])}/answer`;body={actor:option('--actor',{required:true}),...optionalCredentials(),...(option('--decision')?{decision:option('--decision'),note:option('--note')}:{answer:option('--answer',{required:true})})};}
else if(noun==='ticket'&&['takeover','accept','reopen'].includes(verb)&&p[0]){method='POST';route=`/v1/tickets/${encodeURIComponent(p[0])}/${verb}`;body={actor:option('--actor',{required:true}),...(option('--message')?{message:option('--message')}:{})};if(verb!=='accept')auth(headers);}
else if(noun==='event'&&verb==='post'&&p[0]){method='POST';route=`/v1/tickets/${encodeURIComponent(p[0])}/events`;body={...credentials(),message:option('--message',{required:true}),...(option('--metadata')?{metadata:JSON.parse(option('--metadata'))}:{})};}else if(noun==='event'&&verb==='list'){const q=new URLSearchParams();if(option('--project'))q.set('project',option('--project'));if(option('--ticket'))q.set('ticket',option('--ticket'));if(option('--after'))q.set('after',option('--after'));route=`/v1/events?${q}`;}else usage();
const response=await fetch(`${server}${route}`,{method,headers:{'content-type':'application/json',...headers},body:body===undefined?undefined:JSON.stringify(body)});const text=await response.text(),payload=text?JSON.parse(text):{ticket:null};if(!response.ok)throw Object.assign(new Error(payload.error?.message??'request failed'),{payload,status:response.status});if(noun==='question'&&verb==='show')payload.question=payload.questions.find(q=>q.id===p[1])??null;return payload;}
try{const result=await command();process.stdout.write(jsonOutput?`${JSON.stringify(result)}\n`:`${JSON.stringify(result,null,2)}\n`);}catch(error){const payload=error.payload??{error:{code:error.code??'client_error',message:error.message}};process.stderr.write(jsonOutput?`${JSON.stringify(payload)}\n`:`${payload.error.code}: ${payload.error.message}\n`);process.exitCode=error.usage?2:error.status===409?3:error.status===404?4:error.status?5:6;}
