const fragment=location.hash.startsWith('#pair=')?location.hash.slice(6):null;
if(location.hash)history.replaceState(null,'',location.pathname+location.search);
const enc=new TextEncoder(), b64=b=>{let s='';for(const x of new Uint8Array(b))s+=String.fromCharCode(x);return btoa(s).replaceAll('+','-').replaceAll('/','_').replace(/=+$/,'')}, unb=s=>Uint8Array.from(atob(s.replaceAll('-','+').replaceAll('_','/')+'==='.slice((s.length+3)%4)),c=>c.charCodeAt(0));
const lp=(...vs)=>{const parts=vs.map(v=>typeof v==='string'?enc.encode(v):v instanceof ArrayBuffer||ArrayBuffer.isView(v)?new Uint8Array(v.buffer||v,v.byteOffset||0,v.byteLength):enc.encode(String(v))),n=parts.reduce((a,p)=>a+4+p.length,0),out=new Uint8Array(n);let o=0;for(const p of parts){new DataView(out.buffer).setUint32(o,p.length);o+=4;out.set(p,o);o+=p.length}return out};
const db=await new Promise((ok,no)=>{const r=indexedDB.open('viq-phone-auth',1);r.onupgradeneeded=()=>r.result.createObjectStore('keys');r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)});
const get=k=>new Promise((ok,no)=>{const r=db.transaction('keys').objectStore('keys').get(k);r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)}), put=(k,v)=>new Promise((ok,no)=>{const r=db.transaction('keys','readwrite').objectStore('keys').put(v,k);r.onsuccess=()=>ok();r.onerror=()=>no(r.error)});
const state=document.querySelector('#phone-state'),button=document.querySelector('#pair-phone');let saved=await get('device');
async function pair(){button.disabled=true;try{const [intent,secret]=fragment.split('.');if(!intent||!secret)throw new Error('Pairing link is invalid or expired. Ask the operator to revoke and re-pair.');const keys=await crypto.subtle.generateKey({name:'ECDSA',namedCurve:'P-256'},false,['sign','verify']),pub=await crypto.subtle.exportKey('jwk',keys.publicKey),device='dev_'+b64(crypto.getRandomValues(new Uint8Array(18)));const verifier=await crypto.subtle.digest('SHA-256',new Uint8Array([...enc.encode('viq-phone-pair-verifier-v1\0'),...enc.encode(secret)]));const record=lp('viq-phone-pair-record-v1',location.origin,intent,device,pub.x,pub.y),hkey=await crypto.subtle.importKey('raw',verifier,{name:'HMAC',hash:'SHA-256'},false,['sign']),proof=await crypto.subtle.sign('HMAC',hkey,record);const r=await fetch('/__phone/pair',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({intent_id:intent,device_id:device,public_key:{kty:'EC',crv:'P-256',x:pub.x,y:pub.y},proof:b64(proof)})});if(!r.ok)throw new Error('Pairing was denied. Ask the operator to revoke and create a new link.');const result=await r.json();saved={id:device,epoch:result.epoch,key:keys.privateKey};await put('device',saved);await start()}catch(e){state.textContent=e.message;button.disabled=false}}
async function start(){if(!saved?.key){state.textContent='This browser has no usable key. Ask the operator to revoke and re-pair.';return}const native=globalThis.fetch.bind(globalThis);globalThis.fetch=async(input,init={})=>{const request=new Request(input,init),url=new URL(request.url);if(url.origin!==location.origin||!url.pathname.startsWith('/v1/'))return native(request);const raw=request.method==='GET'||request.method==='HEAD'?new Uint8Array():new Uint8Array(await request.clone().arrayBuffer()),bodyHash=await crypto.subtle.digest('SHA-256',raw),target=url.pathname+url.search,cr=await native('/__phone/challenge',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({device_id:saved.id,method:request.method,target,body_hash:b64(bodyHash)})});if(!cr.ok)throw new Error('Device authorization was revoked or lost. Ask the operator to revoke and re-pair.');const c=await cr.json(),record=lp('viq-phone-request-proof-v1',location.origin,c.id,unb(c.nonce),saved.id,c.epoch,request.method,target,new Uint8Array(bodyHash)),sig=await crypto.subtle.sign({name:'ECDSA',hash:'SHA-256'},saved.key,record),headers=new Headers(request.headers);headers.set('x-viq-device',saved.id);headers.set('x-viq-challenge',c.id);headers.set('x-viq-signature',b64(sig));return native(new Request(request,{headers}))};const gate=await globalThis.fetch('/v1/actors?active=true');if(!gate.ok)throw new Error('Device authorization was revoked or lost. Ask the operator to revoke and re-pair.');document.querySelector('#phone-auth').hidden=true;document.querySelector('#phone-app').hidden=false;await import('/app.js')}
if(fragment){button.hidden=false;button.addEventListener('click',pair);state.textContent='The operator invited this browser. Tap once to continue.'}else if(saved)start().catch(e=>state.textContent=e.message);else state.textContent='This browser is not paired. Ask the operator for a pairing link.';
