Phase 2 RED 1 — board queries and assets
Command: node --test test/board-http.test.js
Observed: GET /v1/projects and GET / returned route_not_found 404; 2 failures, exit 1.
GREEN: real storage-backed project/ticket projection and same-server semantic asset tests passed.

Phase 2 browser harness observation
Command: npm run build && node test/browser-e2e.js
The first real Chromium run reached claimed/stale projection and captured both screenshots, then failed because a text locator matched the takeover button and dialog heading. Narrowing the locator to the semantic button allowed the same behavior to complete GREEN. This was a browser-test selector correction, not production behavior.

GREEN browser result: project/ticket created in UI; CLI claim reflected as claimed; expiry reflected as stale and never ready; confirmed authorized takeover reached worker-b generation 2; old owner received stale_claim/exit 3; current owner submitted JSON evidence; submitted detail rendered evidence; BROWSER_E2E_OK.
